# cvtool 실행에 필요한 패키지
#
#   pip install -r requirements.txt
#
# 설치 후 확인:
#   python3 -m cvtool.cli deps
#
# ── 여기 없는 것들 ────────────────────────────────────────────────────
# 웹 서버·엑셀 출력·저장소는 표준 라이브러리만 씁니다.
# FastAPI / uvicorn / openpyxl / psycopg / qdrant-client 는 필요 없습니다.

# --- 추출에 반드시 필요 (없으면 아무것도 안 됨) ---
httpx>=0.27          # vLLM / TEI 호출
pydantic>=2.6        # 추출 결과 검증

# --- 파일 형식 ---
pypdf>=4.0           # .pdf  — 4.0 이상이어야 layout 모드로 2단 편집을 살립니다
python-docx>=1.1     # .docx — 표 안의 내용을 읽는 데 필요

# .txt / .md 는 추가 설치 없이 됩니다.
